<div class="container">
  <div class='d-flex justify-content-between align-items-center py-3'>
    <div class= 'categories index'>
      <p><%= link_to 'Home', '/' %></p>
      <p><%= link_to 'CV', '#' %></p>
      <p><%= link_to 'About me', '#', data: { toggle: 'modal', target: '#exampleModal'} %></p>

      <%# ---- Modal Code ---- %>
      <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title" id="exampleModalLabel">About me</h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close"><i class="fas fa-times"></i></button>
            </div>
            <div class="modal-body">
              <h3>Hi, I'm Lilas. Nice to meet you.</h3>
              <p>I've worked in fashion for 7 years and went through different jobs.
              <br/>
              Even if i was really passionate about what I was doing, I felt like i needed to get deeper into tech world.
              Then I met Le Wagon and i totally fell in love with web programming.
              <br/>
              I'm now looking for being a full-stack developer and maybe match it with my background.</p>
            </div>
          </div>
        </div>
      </div>
      <%# -------------------- %>

      <p><%= link_to 'Contact', '#' %></p>
    </div>
    <h2>My Recent Work</h2>
  </div>
  <div class="grid">
    <% @portfolios.each do |portfolio| %>
      <div class="portfolio-card">
        <%= image_tag portfolio.thumbnail, width: 333, height: 190 %>
        <h4 class='my-2'><%= portfolio.name %></h4>
        <p class='my-1'><%= portfolio.description %></p>
        <p class='mt-4'><%= link_to 'Have a look!', portfolio.url, target: "_blank", class: 'buttons' %></p>
      </div>
    <% end %>
  </div>
</div>
